apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: noteapp-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: noteapp-svc
              servicePort: 2345